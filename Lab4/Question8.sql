Omar Mahmud Feb 19. 15 Lab 4

--A check constraint is used to limt the value range that can
--be placed in a column. If you define a check constraint on a
--single column it only allows a certain vaule for this column.
--If you define a check constraint on a table it can limit the
--values in certain columns based on values in other columns in
--the row. An advantage of check constraints is that is provides
--data integrity and consistency. 
